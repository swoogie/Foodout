import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Food application Test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
